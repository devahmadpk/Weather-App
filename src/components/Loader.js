// src/components/Loader.js
import React from 'react';
import '../stylesheets/loader.css'; // Ensure you have some basic styles for the loader

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
